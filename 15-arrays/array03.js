// length e indexOf

let cores = ['vermelho', 'verde', 'azul']

console.log(`O array tem ${cores.length} posições`)

let pos = cores.indexOf('verde')
console.log(`A cor 'verde' está na posição ${pos}`)